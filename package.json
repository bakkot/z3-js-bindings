{
  "scripts": {
    "build-ts-wrapper": "node scripts/make-ts-wrapper.js > build/wrapper.ts && tsc --noEmit build/wrapper.ts",
    "build": "npm run build-ts-wrapper && tsc"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  }
}
